# whatsapp-adapter

Coopfuturo whatsapp-adapter (MOCK provider)

Architecture foundation only — **no commercial product endpoints**.

Technical routes under `/_tech/*` (auth required) for outbox/inbox/probes.

Health: `/health/live`, `/health/ready`, `/version`
