"""agent_config domain service — implementación de dominio en fases posteriores."""

from __future__ import annotations


class AgentConfigService:
    """Placeholder tipado; sin side-effects."""

    name: str = "agent_config"

    def ping(self) -> str:
        return self.name
