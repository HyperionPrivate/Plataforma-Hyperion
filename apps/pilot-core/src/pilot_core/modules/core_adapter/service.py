"""core_adapter domain service — implementación de dominio en fases posteriores."""

from __future__ import annotations


class CoreAdapterService:
    """Placeholder tipado; sin side-effects."""

    name: str = "core_adapter"

    def ping(self) -> str:
        return self.name
