{
  "name": "@hyperion/nova-bff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @hyperion/nova-contracts build",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "pretest": "pnpm --filter @hyperion/nova-contracts build",
    "test": "vitest run",
    "pretypecheck": "pnpm --filter @hyperion/nova-contracts build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.1.0",
    "@hyperion/nova-contracts": "1.1.0",
    "@hyperion/platform-contracts": "1.1.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^7.0.2"
  }
}
